import {
  anything,
  capture,
  instance,
  mock,
  spy,
  verify,
  when,
} from "@typestrong/ts-mockito";


describe("Register", () => {
    // vars

    beforeEach(() => {
        // setup
    });

    it("sample test", () => {
        expect(true).toBe(true);
    });
});